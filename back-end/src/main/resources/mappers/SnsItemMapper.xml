<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.back_end.sns.mapper.SnsItemMapper">
    <resultMap id="itemList" type="com.ssafy.back_end.sns.model.ItemDetailDto">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="itemId" column="item_id"/>
        <result property="detailType" column="detail_type"/>
    </resultMap>

    <delete id="deleteItem">
        DELETE
        FROM items
        WHERE id = #{id};
    </delete>

    <select id="isLike" resultType="int">
        SELECT COUNT(*)
        FROM item_details
        WHERE item_id = #{itemId}
          AND user_id = #{userId}
          AND detail_type = "like";
    </select>

    <insert id="likeItem">
        INSERT INTO item_details (item_id, user_id, detail_type)
        VALUES (#{itemId}, #{userId}, "like");
    </insert>

    <delete id="dislikeItem">
        DELETE
        FROM item_details
        WHERE item_id = #{itemId}
          AND user_id = #{userId}
          AND detail_type = "like";
    </delete>
</mapper>